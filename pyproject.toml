[project]
name = "plan-based-researcher"
version = "0.1.0"
description = "Plan-based multi-agent AI researcher backend"
requires-python = ">=3.12"
dependencies = [
    "arxiv>=2.2.0",
    "chainlit>=2.6.0",
    "fastapi[standard]>=0.141.1",
    "httpx>=0.28.0",
    "langchain>=1.3.17",
    "langchain-classic>=1.0.8",
    "langchain-community>=0.4.0",
    "langchain-openai>=1.6.0",
    "langchain-text-splitters>=1.0.0",
    "langgraph>=1.2.11",
    "langgraph-checkpoint-postgres>=3.0.0",
    "pgvector>=0.4.0",
    "psycopg[binary,pool]>=3.2.0",
    "pydantic-settings>=2.10.0",
    "pymupdf>=1.26.0",
    "python-dotenv>=1.2.3",
    "rank-bm25>=0.2.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/plan_based_researcher"]
